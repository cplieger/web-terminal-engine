name: Release
on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: cplieger/ci/.github/workflows/release.yaml@bd60d228e855bfe73901919383b8750790fbbece # v1
    with:
      target: ts
      working-directory: web
